# Tiny LLM engine: live module status

This file is the short operational status companion to `ENGINEERING_GUIDE.md`. It records what is implemented and validated, rather than the aspirational end state.

| Module | State | Evidence | Limitation / next step |
|---|---|---|---|
| C99 ABI and C++17 control plane | PASS | Clean CMake build, focused CTest suite, and explicit `weight_policy=preserve` default | `quantize-cache` is parsed but returns named unsupported until a calibrated converter exists |
| CLI configuration and probes | PASS FOR NARROW GENERATION AND LOCAL HTTP ENTRYPOINT | CLI parsing, resolved configuration, trace/probe tests, help text, `--generate --prompt --max-new-tokens` path, `--server --port`, loopback HTTP health/completion/chat routes, and bounded JSON handling are implemented; generation validates every layer’s exact matrix/norm shapes, native type, head topology, and maximum bound scratch | Server accepts only the narrow validated model profile, is single-process/single-request sequential, binds loopback by design, and has no authentication or arbitrary checkpoint support |
| Tensor dtype/view/buffer layer | PASS | Dense views plus native packed-block views validate exact byte geometry; concrete GGML Q4_0/Q8_0/Q4_K validation and direct CPU dot execution pass without expansion; SafeTensors F32 matrix matvec reads exact source bytes into caller scratch | Native model binding still reads into caller-owned storage; remaining K-quants and non-contiguous views remain unsupported |
| GGUF inspection | PASS FOR BOUNDED LLAMA PROFILE AND MODEL-AWARE SPLIT SETS | v3 header, bounded metadata, alignment, indexed tensor queries, exact bounded payload validation for native Q4_0/Q8_0/Q4_K descriptors, exact GGUF vocabulary, bounded llama architecture scalar query, lazy binding/read views, expert-count, experts-per-token, split.count/split.no/split.tensors.count validation, duplicate-name rejection, per-shard header ownership, and shard-one native execution tests | Other quantized block types, architecture families, complete expert grouping, and automatic model manifest discovery remain pending; model tensor lookup on a split set must use the index-aware API and the execution scheduler is not shard-resident |
| SafeTensors inspection/execution | PASS FOR BOUNDED F32 CPU/VULKAN GRAPH AND EXPLICIT FILE SETS | Valid descriptor, dtype/shape byte-size, file-bound, non-overlap rejection, indexed tensor queries, metadata-bound Llama scalar architecture, contiguous `tokenizer.token.N` vocabulary, explicit multi-file shard binding with duplicate-name rejection and metadata consistency, caller-scratch F32 matvec CPU/Vulkan differential, reusable-context repeated F32 MLP differential, full-role F32 MLP/logits graph, one-step transformer differential, and numeric-token generation fixture pass without source conversion | Standard index-file discovery, tied weights, standard tokenizer.json/BPE semantics, and explicit quantize-cache conversion remain unsupported; the Vulkan F32 context is scalar and host-visible, not a complete device-resident model graph |
| CPU scalar math | PASS FOR NARROW NATIVE PROFILE | F32 dot/softmax, decoder golden-logit, direct packed Q4_0/Q8_0/Q4_K dot and matvec tests, SafeTensors F32 matrix matvec and full one-layer transformer/generation fixture, selected-expert F32 and native Q4_K MLP fixtures, deterministic sampling, and model-bound native MLP-to-logits/attention/multi-layer generation differential fixtures pass | Broad operator coverage, full tokenizer semantics, and arbitrary checkpoint generation are not implemented |
| Paged KV control plane | PASS FOR BOUNDED OPAQUE AND TYPED CODEC PAYLOADS | Metadata-only creation remains compatible; explicit payload creation allocates separate per-page key/value byte buffers, bounded read/write operations detach shared pages before mutation, rollback clears truncated bytes, and release/reuse plus overflow/bounds/COW tests pass; typed F16/BF16/Q8/Q6/Q4 creation, encoded layout sizing, F32 round-trip tolerances, invalid-value rejection, and copy-on-write tests pass | Typed codecs are CPU conversion boundaries and are not yet wired into native transformer KV allocation; no prefix identity/lookup, eviction, migration, import/export, device residency, or accelerated typed-KV kernels |
| Replaceable kernel registry | PASS | CPU/Vulkan scalar/DP4/Q4_K/Q8_0 selection, dtype/alignment contracts, and generic operation dispatch tests | Contract metadata is centralized; no full tensor graph scheduler or routed-expert Vulkan dispatcher yet |
| Vulkan device discovery | PASS | Physical-device and queue discovery on llvmpipe; explicit runtime selection validates index/capability | No target discrete GPU validation; no memory planner |
| Vulkan packed-int8 DP4 dispatch | PASS | Real compute dispatch and generic contract dispatch differentially return expected result on llvmpipe; registry contract declares I8→I32 and 4-byte alignment | Native routed-expert model integration, benchmark, and discrete-GPU validation pending |
| Vulkan native Q4_K dispatch | PASS FOR PACKED DOT/MATVEC/REUSABLE CONTEXT FIXTURE | Replaceable native-packed Q4_K dot and one-command multi-row matvec shaders/dispatches plus reusable packed context buffer reuse pass CPU differentials on llvmpipe; capability-driven Q4_K dot contract selects `vulkan/q4_k` | Matvec uses a dedicated contract because row stride is explicit; no persistent packed model graph, routed-expert Vulkan dispatch, benchmark, or discrete-GPU validation yet |
| Vulkan native Q8_0 dispatch | PASS FOR PACKED DOT/MATVEC/REUSABLE CONTEXT/NARROW MULTI-LAYER GQA PROFILE | Replaceable native-packed Q8_0 dot and one-command multi-row matvec shaders/dispatches, reusable packed context buffer reuse, kernel contract, generic dispatch, model-bound lazy-span helpers, Vulkan-first automatic runtime resolution, and the native multi-layer GQA MLP/attention/logits/generation fixture pass CPU differentials on llvmpipe | No heterogeneous architecture support, persistent packed model graph, benchmark, or discrete-GPU validation yet |
| Cooperative matrices | EXCLUDED | No implementation source or registry path | Remains excluded by design |
| Dense decoder tensor mapping/graph plan | PASS FOR NARROW MULTI-LAYER GQA PROFILE | Exact llama.cpp-style global and `blk.N.*` tensor names map to explicit roles; bounded GGUF llama architecture scalars are parsed and queried; plan builder and positive GGUF fixture require token/output norms plus all nine roles for every contiguous layer; the native profile validates matrix format, rank, dimensions, F32 norm vectors, bounds, scratch capacity, layer index, and executes the bound multi-layer path | Architecture metadata is only the exact llama scalar contract; heterogeneous architectures and layer shapes remain unsupported; tested attention topology is two query heads with one KV head |
| Native attention and generation | PASS FOR NARROW MULTI-LAYER GQA PROFILE | Native Q8_0 attention projections/output for each validated layer, exact llama architecture metadata query, optional RoPE, causal softmax, explicit two-query-head/one-KV-head GQA fixture, one F32 KV payload per layer, prompt-token stepping, deterministic sampling, stop-token handling, and text decode pass CPU/Vulkan fixture differentials on a two-layer checkpoint fixture | Deliberately narrow: validated same-shape llama metadata, limited GQA topology, F32 KV payload, no architecture variants, batching, streaming, or server |
| Exact tokenizer contracts | PASS FOR BOUNDED VOCABULARY AND RAW-UTF8 BPE CONTRACTS | GGUF `tokenizer.ggml.tokens` and SafeTensors contiguous `tokenizer.token.N` string metadata are bounded, stored exactly, and tested with token lookup, greedy longest-match encoding, decoding, capacity, and invalid-token cases; standalone tokenizer.json BPE parser validates vocabulary IDs, ordered merge ranks, UTF-8 symbols, unknown-token fallback, decode bounds, and malformed/unsupported pipeline cases | Byte-level BPE, Unicode normalization, pre-tokenization, post-processing, special-token policy, tokenizer association/index discovery, WordPiece, Unigram, SentencePiece, and arbitrary external vocabulary contracts remain unsupported |
| Tiny scalar decoder fixture | PASS | Single-layer/single-head embedding, RMSNorm, optional RoPE, causal attention, residuals, SiLU MLP, logits, reset, and bounds are golden-tested | Deliberately narrow: no multi-layer/GQA mapping, quantized weights, or broad Llama compatibility |
| CPU MoE routing oracle | PASS FOR NARROW CHECKPOINT-BOUND SLICE | Deterministic top-k selection, tie-breaking, all-softmax/selected-only weighting, normalization, selected-output combination, NaN/bound checks, explicit Mixtral-style expert-name mapping, SafeTensors F32 router logits into deterministic routing, strict Mixtral graph discovery, checkpoint-level F32-router plus native Q4_K expert execution, selected-expert F32/native Q4_K MLP fixtures, and a model-bound rank-3 Q4_K fixture that stages only routed expert spans pass | Full model-specific router semantics, capacity policies, heterogeneous expert formats, and Vulkan routed-expert execution remain unsupported; the validated rank-3 contract is expert-major packed storage with expert axis 2 and exact Q4_K strides |
| Lazy/disk-backed residency | PASS FOR BOUNDED HOST RESIDENCY AND MODEL-AWARE FILE SETS | Model handles bind validated data bases to range-checked relative tensor spans; Unix file handles use demand-paged read-only mmap when representable and fall back to bounded stream reads; single-window prefetch/read and contiguous cross-shard reads are bounded and tested; GGUF split sets and explicit SafeTensors file sets resolve per-tensor headers and exact caller-buffer reads without source conversion or whole-file materialization; reusable F32 and packed Vulkan contexts retain device resources and grow buffers on demand | Model-level shard manifests, asynchronous prefetch, residency traces, eviction, packed model-graph residency, and a full execution scheduler remain unsupported; current contexts use host-visible staging buffers |
| OpenAI/Llama server and WebUI adapter | PASS FOR BOUNDED LOCAL HTTP ADAPTER | Loopback `--server` mode exposes `/health`, `/v1/completions`, and `/v1/chat/completions`; JSON prompt/max-token/stream fields are bounded, non-streaming responses are JSON, streaming responses use SSE framing, and the library exposes token callbacks plus a sequential text-batch wrapper | No authentication, TLS, conversation-role parser, cancellation socket, concurrent scheduler, true parallel batching, WebUI assets, or arbitrary model compatibility |
| Context rolling/compression | PASS FOR EXTRACTIVE BOUNDED POLICY | Native generation can reject overflow or preserve a caller-selected prefix plus recent tail within a configured window; token and tokenizer-backed text compaction are deterministic, bounded, and tested | No learned summarizer, semantic compression, retrieval index, or claim of extending a model’s trained context; dropped middle tokens are explicit policy behavior |
| Direct ROCr/HSA / custom AMDGPU | PASS FOR RUNTIME PROBE; EXECUTION UNSUPPORTED | Dynamic probe checks `libhsa-runtime64` candidates, resolves `hsa_init`/`hsa_shut_down`, initializes and tears down the real runtime when present, and reports the exact library state; this host has no ROCr library, `/dev/kfd`, or AMDGPU render device, so the regression takes the named unsupported branch | No HSA queue/code-object compiler or direct model matvec execution is claimed; adding that path requires a real ROCr-capable host and validated AMDGPU code object, with no HIP fallback |

## Validation snapshot

Fresh clean Vulkan-enabled and no-Vulkan Debug builds passed CTest; the Vulkan run used the available llvmpipe Vulkan CPU implementation and included valid/malformed GGUF and SafeTensors fixtures, model-aware GGUF split-set and explicit SafeTensors file-set opening, duplicate/missing/misnumbered shard rejection, per-shard tensor spans, shard-one native Q8_0 CPU/Vulkan matvec differential, indexed model descriptors, exact native GGUF Q4_0/Q8_0/Q4_K payload bounds, model-bound lazy caller-buffer Q4_0/Q8_0/Q4_K execution helpers, model-backed dense graph binding, strict native MLP and attention profile shape/type/scratch checks, CPU/Vulkan model-bound MLP-to-logits and attention/KV differentials, multi-layer transformer execution with per-layer KV caches on a two-layer GQA fixture, SafeTensors metadata/tokenizer parsing, SafeTensors F32 CPU/Vulkan MLP plus repeated reusable F32-context dispatch, native Q4_K/Q8_0 reusable-context differentials, CPU transformer/generation with architecture metadata, token callbacks, sequential batch generation, and local HTTP health smoke coverage, exact GGUF vocabulary parsing, raw-UTF8 tokenizer.json BPE merge-rank tests, bounded native prompt-to-text generation with deterministic sampling and stop handling, CLI generation argument coverage, backend-selecting model-native Q8_0 matvec, file-span bounds, the narrow dense graph-plan fixture, the tiny decoder golden-logit fixture, explicit Mixtral mapping, selected-expert F32 and native Q4_K CPU MLP fixtures, model-bound rank-3 Q4_K selected-expert span staging against the CPU oracle, SafeTensors F32 router logits into the MoE oracle, strict Mixtral graph discovery, checkpoint-level F32-router plus Q4_K expert execution, the MoE oracle, bounded file windows, cross-shard reads, token compaction, rolling-tail overflow policy, ROCr runtime probe, quantized tensor contracts, generic scalar/DP4/Q4_K/Q8_0 dispatch, native Q4_K/Q8_0 Vulkan/CPU differential dot and one-command multi-row matvec fixtures, bounded opaque KV payload COW/read/write/rollback/reuse tests, and typed F16/BF16/Q8/Q6/Q4 KV codec round trips with invalid-input and COW checks. All generated shader modules contained compute entry points; named no-Vulkan stubs also passed. Implementation source measurement is refreshed at each commit after GQA attention, SafeTensors metadata/tokenizer and F32 CPU/Vulkan graph execution, strict architecture binding, Vulkan-first runtime additions, model-bound MoE staging, all-layer CLI validation, demand-paged file access, reusable Vulkan contexts, bounded windows, cross-shard reads, model-aware shard ownership, and typed KV codecs. Discrete-GPU behavior/performance remains unvalidated; arbitrary architecture variants, byte-level/normalized tokenizer semantics and special-token policy, prefix-aware native KV reuse, device-resident typed-KV/model graphs, concurrent server scheduling, authentication, WebUI assets, arbitrary checkpoint handling, distributed batching, and learned semantic context compression remain incomplete. Cooperative matrices remain absent from implementation paths.
